import { FilterCollectionPipe } from './filter-collection.pipe';

describe('FilterCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});
